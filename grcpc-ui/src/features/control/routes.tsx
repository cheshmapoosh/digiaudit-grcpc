import { Route } from "react-router-dom";
import CentralControlsPage from "./pages/CentralControlsPage";
export const centralControlRoutes = (
  <Route path="/controls" element={<CentralControlsPage />} />
);
