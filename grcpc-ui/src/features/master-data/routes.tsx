import { Route } from "react-router-dom";

import MasterDataFeaturePage from "./pages/MasterDataFeaturePage";

export const masterDataRoutes = (
    <Route path="master-data" element={<MasterDataFeaturePage />} />
);
